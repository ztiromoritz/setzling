{
  "name": "setzling-client",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "run-s build:main build:rollup",
    "build:main": "tsc -p tsconfig.json",
    "build:module": "tsc -p tsconfig.module.json",
    "build:rollup": "rollup -c",
    "watch": "run-p watch:*",
    "watch:main": "tsc -w -p tsconfig.json",
    "watch:module": "tsc -w -p tsconfig.module.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@rollup/plugin-commonjs": "^16.0.0",
    "@rollup/plugin-node-resolve": "^10.0.0",
    "@rollup/plugin-replace": "^2.3.4",
    "npm-run-all": "^4.1.5",
    "rollup": "^2.33.2",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "immer": "^7.0.14",
    "pixi.js": "^5.3.3",
    "pixi.js-legacy": "^5.3.3",
    "setzling-common": "file:../common/"
  }
}
